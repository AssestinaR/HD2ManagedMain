<UserControl x:Class="HD2ModManager.Views.HomePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:HD2ModManager.ViewModels"
             xmlns:res="clr-namespace:HD2ModManager.Resources"
             xmlns:views="clr-namespace:HD2ModManager.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <DataTemplate DataType="{x:Type vm:NavCardViewModel}">
            <views:NavCard/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ActionCardViewModel}">
            <Button Command="{Binding ActionCommand}"
                    Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" FocusVisualStyle="{x:Null}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Button.Template>
                <Border Style="{DynamicResource CardContainer}" Width="260">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Rectangle Grid.RowSpan="2" Width="40" Height="40" Fill="#E0E0E0" Stroke="#C8C8C8" RadiusX="6" RadiusY="6"/>
                        <TextBlock Grid.Column="1" Text="{Binding Title}" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Description}" Foreground="#666" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        <TextBlock Grid.Column="2" Grid.RowSpan="2" Text="+" Foreground="#888" FontSize="16" VerticalAlignment="Center" Margin="8,0,2,0"/>
                    </Grid>
                </Border>
            </Button>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProfileCardViewModel}">
            <Border Style="{DynamicResource CardContainer}" Width="260">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Column="0">
                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                        <TextBlock Text="  "/>
                        <TextBlock Text="{x:Static res:Strings.Profile_Active}" Foreground="#2E7D32" Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Grid.ColumnSpan="2" Margin="0,6,0,0">
                        <Button Content="{x:Static res:Strings.Profile_Enable}" Command="{Binding EnableCommand}" Margin="0,0,6,0"/>
                        <Button Content="{x:Static res:Strings.Profile_Disable}" Command="{Binding DisableCommand}" Margin="0,0,6,0"/>
                        <Button Content="{x:Static res:Strings.Profile_Rename}" Command="{Binding RenameCommand}" Margin="0,0,6,0"/>
                        <Button Content="{x:Static res:Strings.Profile_Delete}" Command="{Binding DeleteCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingToggleCardViewModel}">
            <views:SettingToggleCard/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StatusCardViewModel}">
            <Border Style="{DynamicResource CardContainer}" Width="260">
                <StackPanel>
                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Text}" Foreground="#666"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8">
            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="  ("/>
            <TextBlock Text="{Binding Cards.Count}"/>
            <TextBlock Text=")"/>
        </StackPanel>
        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding Cards}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.Style>
                    <Style TargetType="ItemsControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Cards.Count}" Value="0">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ItemsControl">
                                            <TextBlock Text="No cards" Foreground="#888" Margin="12"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Style>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
