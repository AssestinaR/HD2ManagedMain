<UserControl x:Class="HD2ModManager.Views.SettingsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:HD2ModManager.Resources"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <ScrollViewer Background="Transparent">
        <StackPanel>
            <TextBlock Text="{x:Static Member=res:Strings.Home_Settings}" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12"/>

            <Border Style="{StaticResource CardContainer}" Margin="{StaticResource CardStackMargin}">
                <StackPanel>
                    <TextBlock Text="通用" Style="{StaticResource SectionTitle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <TextBlock Text="Language" Width="150" VerticalAlignment="Center"/>
                        <ComboBox Width="180" SelectedValue="{Binding Language, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag">
                            <ComboBoxItem Content="English" Tag="en-US"/>
                            <ComboBoxItem Content="中文" Tag="zh-CN"/>
                        </ComboBox>
                    </StackPanel>
                    <CheckBox Content="启用自动清理（启动时清理无效项）" IsChecked="{Binding AutoCleanup, Mode=TwoWay}" Margin="0,12,0,0"/>
                    <CheckBox Content="启用库页图片" IsChecked="{Binding EnableLibraryImages, Mode=TwoWay}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardContainer}" Margin="{StaticResource CardStackMargin}">
                <StackPanel>
                    <TextBlock Text="资产信息" Style="{StaticResource SectionTitle}"/>
                    <CheckBox Content="自动更新资产信息" IsChecked="{Binding AutoUpdateAssetMetadata, Mode=TwoWay}" Margin="0,12,0,0"/>
                    <TextBlock Text="仓库 Raw 地址或 GitHub 仓库地址" Foreground="#666" Margin="0,12,0,4"/>
                    <DockPanel>
                        <Button DockPanel.Dock="Right" Content="立即更新资产" Command="{Binding UpdateAssetMetadataCommand}" Margin="8,0,0,0"/>
                        <TextBox Text="{Binding AssetMetadataRepository, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>
                    <TextBlock Text="{Binding AssetMetadataStatus}" Foreground="#666" TextWrapping="Wrap" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardContainer}" Margin="{StaticResource CardStackMargin}">
                <StackPanel>
                    <TextBlock Text="Mod Library Folder" Style="{StaticResource SectionTitle}"/>
                    <DockPanel Margin="0,10,0,0">
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="8,0,0,0">
                            <Button Content="Browse" Click="BrowseModFolder_Click"/>
                            <Button Content="Open" Command="{Binding OpenModFolderCommand}" Margin="8,0,0,0"/>
                            <Button Content="Reset" Command="{Binding ResetModFolderCommand}" Margin="8,0,0,0"/>
                        </StackPanel>
                        <TextBox Text="{Binding ModLibraryFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardContainer}">
                <StackPanel>
                    <TextBlock Text="Game Data Folder" Style="{StaticResource SectionTitle}"/>
                    <DockPanel Margin="0,10,0,0">
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="8,0,0,0">
                            <Button Content="Browse" Click="BrowseGameDataFolder_Click"/>
                            <Button Content="Open" Command="{Binding OpenGameDataFolderCommand}" Margin="8,0,0,0"/>
                            <Button Content="Detect" Command="{Binding DetectGameDataFolderCommand}" Margin="8,0,0,0"/>
                            <Button Content="查看资产索引" Command="{Binding ViewGameDataIndexCommand}" Margin="8,0,0,0"/>
                        </StackPanel>
                        <TextBox Text="{Binding GameDataFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </DockPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
