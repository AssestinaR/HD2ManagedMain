<UserControl x:Class="HD2ModManager.Views.GameDataBrowserPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HD2ModManager.Views">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="210"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Style="{StaticResource CardContainer}" Margin="{StaticResource CardStackMargin}">
            <StackPanel>
                <TextBlock Text="Game Data 资产" Style="{StaticResource AppCardTitle}"/>
                <TextBlock Text="选择 archive 后，在右侧按需读取 AssetKey 详情。" Style="{StaticResource AppSupportingText}" Margin="0,3,0,0"/>
                <TextBlock Text="{Binding Summary}" Style="{StaticResource AppSupportingText}" Margin="0,8,0,0"/>
                <TextBlock Text="{Binding BuiltText}" Style="{StaticResource AppSupportingText}" Margin="0,3,0,0"/>
                <TextBlock Text="{Binding SourceText}" Style="{StaticResource AppSupportingText}" TextTrimming="CharacterEllipsis" Margin="0,3,0,0" ToolTip="{Binding SourceText}"/>
                <TextBlock Text="{Binding ProfileText}" Style="{StaticResource AppSupportingText}" Margin="0,3,0,0"/>
                <Border Style="{StaticResource DataTableToolbar}" Margin="0,8,0,0" Padding="0">
                <StackPanel Orientation="Horizontal">
                    <TextBox Style="{StaticResource AppSearchBox}" Width="180" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="搜索显示名、Package、Category 或 Mod"/>
                    <ComboBox Style="{StaticResource AppCompactComboBox}" Width="120" ItemsSource="{Binding CategoryFilters}" SelectedItem="{Binding CategoryFilter}" Margin="8,0,0,0"/>
                    <ComboBox Style="{StaticResource AppCompactComboBox}" Width="120" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding StatusFilter}" Margin="8,0,0,0"/>
                </StackPanel>
                </Border>
            </StackPanel>
        </Border>
        <Grid Grid.Row="1">
            <Border Style="{StaticResource DataTableCard}">
                <DataGrid Style="{StaticResource DataTableGrid}" ItemsSource="{Binding ArchivesView}" SelectedItem="{Binding SelectedArchive, Mode=TwoWay}" SelectionChanged="OnSelectionChanged">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                            <Setter Property="ToolTipService.ShowDuration" Value="30000"/>
                            <Setter Property="ToolTip">
                                <Setter.Value>
                                    <ToolTip>
                                        <StackPanel MaxWidth="520">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding PackageName, StringFormat=Package：{0}}" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding Category, StringFormat=分类：{0}}"/>
                                            <TextBlock Text="{Binding EntryCount, StringFormat=AssetKey：{0}}"/>
                                            <TextBlock Text="{Binding ReplacementStatus, StringFormat=替换状态：{0}}"/>
                                            <TextBlock Text="{Binding EffectivePatchGroup, StringFormat=生效 patch 组：{0}}" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding EffectiveMod, StringFormat=生效 Mod：{0}}" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </ToolTip>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="对象" Binding="{Binding DisplayName}" Width="1.6*"/>
                        <DataGridTextColumn Header="Package" Binding="{Binding PackageName}" Width="1.15*"/>
                        <DataGridTextColumn Header="分类" Binding="{Binding Category}" Width="0.8*"/>
                        <DataGridTextColumn Header="AssetKey" Binding="{Binding EntryCount}" Width="0.65*"/>
                        <DataGridTextColumn Header="替换状态" Binding="{Binding ReplacementStatus}" Width="0.9*"/>
                        <DataGridTextColumn Header="生效 patch 组" Binding="{Binding EffectivePatchGroup}" Width="1.15*"/>
                        <DataGridTextColumn Header="生效 Mod" Binding="{Binding EffectiveMod}" Width="1.1*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            <Border Style="{StaticResource DataTableStatePanel}" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" VerticalAlignment="Center" Margin="24">
                <TextBlock Text="{Binding LoadState}" TextWrapping="Wrap" HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>