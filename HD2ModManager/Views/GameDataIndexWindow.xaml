<Window x:Class="HD2ModManager.Views.GameDataIndexWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GameData 资产索引" Width="1220" Height="720"
        MinWidth="900" MinHeight="480" WindowStartupLocation="CenterOwner"
        Background="#F5F6F8">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel><TextBlock Text="GameData 资产索引" FontSize="20" FontWeight="SemiBold"/><TextBlock Text="双击对象查看 AssetKey 详情" Foreground="#777" Margin="0,3,0,0"/></StackPanel>
            <TextBlock Grid.Column="1" Text="{Binding ProfileText}" Foreground="#555" VerticalAlignment="Center"/>
        </Grid>
        <StackPanel Grid.Row="1" Margin="0,10,0,12">
            <TextBlock Text="{Binding Summary}" Foreground="#555"/>
            <TextBlock Text="{Binding BuiltText}" Foreground="#777" Margin="0,3,0,0"/>
            <TextBlock Text="{Binding SourceText}" Foreground="#777" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
        </StackPanel>
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="170"/><ColumnDefinition Width="170"/></Grid.ColumnDefinitions>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="9,6" ToolTip="搜索显示名、Package、Category 或 Mod"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding CategoryFilters}" SelectedItem="{Binding CategoryFilter}" Margin="8,0,0,0" Padding="7,5"/>
            <ComboBox Grid.Column="2" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding StatusFilter}" Margin="8,0,0,0" Padding="7,5"/>
        </Grid>
        <Border Grid.Row="3" Style="{StaticResource CardContainer}" Padding="0">
            <ListView ItemsSource="{Binding ArchivesView}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                <ListView.ItemContainerStyle><Style TargetType="ListViewItem"><EventSetter Event="MouseDoubleClick" Handler="OnRowDoubleClick"/></Style></ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView AllowsColumnReorder="True">
                        <GridViewColumn HeaderContainerStyle="{x:Null}" Width="250"><GridViewColumn.Header><GridViewColumnHeader Content="Display name" Tag="DisplayName" Click="OnHeaderClick"/></GridViewColumn.Header><GridViewColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis"/></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                        <GridViewColumn HeaderContainerStyle="{x:Null}" Width="165"><GridViewColumn.Header><GridViewColumnHeader Content="Package" Tag="PackageName" Click="OnHeaderClick"/></GridViewColumn.Header><GridViewColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding PackageName}"/></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                        <GridViewColumn HeaderContainerStyle="{x:Null}" Width="120"><GridViewColumn.Header><GridViewColumnHeader Content="Category" Tag="Category" Click="OnHeaderClick"/></GridViewColumn.Header><GridViewColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding Category}"/></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                        <GridViewColumn HeaderContainerStyle="{x:Null}" Width="110"><GridViewColumn.Header><GridViewColumnHeader Content="AssetKey count" Tag="EntryCount" Click="OnHeaderClick"/></GridViewColumn.Header><GridViewColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding EntryCount}" Foreground="{Binding AssetCountForeground}" Padding="8,3" HorizontalAlignment="Center"/></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                        <GridViewColumn HeaderContainerStyle="{x:Null}" Width="125"><GridViewColumn.Header><GridViewColumnHeader Content="替换状态" Tag="ReplacementStatus" Click="OnHeaderClick"/></GridViewColumn.Header><GridViewColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding ReplacementStatus}" Foreground="{Binding ReplacementForeground}" FontWeight="SemiBold" Padding="8,3" HorizontalAlignment="Center"/></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                        <GridViewColumn HeaderContainerStyle="{x:Null}" Width="220"><GridViewColumn.Header><GridViewColumnHeader Content="生效的 patch 组" Tag="EffectivePatchGroup" Click="OnHeaderClick"/></GridViewColumn.Header><GridViewColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding EffectivePatchGroup}" TextTrimming="CharacterEllipsis"/></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                        <GridViewColumn HeaderContainerStyle="{x:Null}" Width="200"><GridViewColumn.Header><GridViewColumnHeader Content="生效的 Mod" Tag="EffectiveMod" Click="OnHeaderClick"/></GridViewColumn.Header><GridViewColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding EffectiveMod}" TextTrimming="CharacterEllipsis"/></DataTemplate></GridViewColumn.CellTemplate></GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</Window>
